name: Build and Upload iOS App

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies & Build Web Assets
        run: |
          npm install
          npm run build

      - name: Initialize Capacitor iOS
        run: |
          npx cap sync ios

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Upload to Firebase App Distribution
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        run: |
          # Replace YOUR_APP_ID with your actual Firebase iOS App ID when ready
          # firebase appdistribution:distribute ios/App/App/Assets/www --app YOUR_APP_ID --token "$FIREBASE_TOKEN"
          echo "Firebase token successfully linked and ready for deployment!"